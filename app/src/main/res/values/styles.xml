<resources>

    <!-- Base application theme. -->
    <style name="OurnetTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:colorBackgroundCacheHint">@android:color/transparent</item>
        <item name="android:windowBackground">@color/colorLayout</item>
        <item name="android:textViewStyle">@style/OurnetTheme.TextAppearance</item>
    </style>

    <style name="OurnetTheme.TextAppearance" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/colorTextPrimary</item>
    </style>

</resources>
